version: "2"
services:
  peons-app:
    build: .
    container_name: peons
    ports:
      - "80:80"
    volumes:
      - ./src:/app/src
      - ./server:/app/server
      - ./build:/app/build
    env_file:
      - ./.env.production.local
    restart: always