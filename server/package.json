{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon -r dotenv-flow/config server.js --node-env=development --dotenv-flow-path=../",
    "prod": "nodemon -r dotenv-flow/config server.js --node-env=production --dotenv-flow-path=../",
    "start": "nodemon -r dotenv-flow/config server.js --node-env=production --dotenv-flow-path=../"
  },
  "dependencies": {
    "dotenv-flow": "^3.2.0",
    "express": "4.17.1",
    "nodemon": "2.0.3",
    "socket.io": "^2.3.0"
  }
}
