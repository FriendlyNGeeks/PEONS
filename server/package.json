{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "engines": {
    "node": ">=14.15.1"
  },
  "scripts": {
    "dev": "nodemon -r dotenv-flow/config server.js --node-env=development --dotenv-flow-path=../",
    "prod": "nodemon -r dotenv-flow/config server.js --node-env=production --dotenv-flow-path=../",
    "start": "node -r dotenv-flow/config server.js --node-env=production --dotenv-flow-path=../",
    "test": "echo \"Only a test\" && exit 1"
  },
  "dependencies": {
    "dotenv-flow": "^3.2.0",
    "express": "4.17.1",
    "nodemon": "2.0.3",
    "socket.io": "^2.3.0"
  }
}
